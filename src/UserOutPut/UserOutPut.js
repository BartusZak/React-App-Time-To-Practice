import React from 'react';

const useroutput = (props) => {
    return (
        <div>
            <p>{props.username}</p>
            <p>Drugi paragraf</p>
        </div>
    )
};

export default useroutput;